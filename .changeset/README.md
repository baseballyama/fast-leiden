# Changesets

This directory tracks pending version bumps. When you make a user-facing
change, run `pnpm changeset` to add a markdown file describing the change.

The release workflow consumes these files to bump versions and update
`CHANGELOG.md` automatically. See
[the Changesets docs](https://github.com/changesets/changesets) for details.
